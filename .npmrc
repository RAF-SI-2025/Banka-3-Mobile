# Expo's own devtools peer graph (radix/vaul wanting a newer react-dom
# than the SDK-pinned react) plus third-party libs make strict peer
# resolution unworkable here. legacy-peer-deps is the standard, Expo-
# recommended setting for managed projects with extra dependencies.
legacy-peer-deps=true
